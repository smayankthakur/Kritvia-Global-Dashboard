{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "apps/web/.next/**",
        "apps/web/next-env.d.ts",
        "apps/web/.vercel/**",
        "apps/web/public/**",
        "dist/**",
        "!.next/cache/**"
      ]
    }
  }
}
